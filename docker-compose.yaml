
services:
  redis:
    image: redis:7-alpine
    container_name: chai-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data
    
  mongo:
    image: mongo:7
    container_name: chai-mongo
    environment:
      MONGO_INITDB_DATABASE: chai_aur_redis
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  redis-data:
  mongo-data:
